// Model - Data structure for the shopping list
var shoppingList = [];

// Function to add item to the shopping list
function addItemToList(item) {
    shoppingList.push(item);
}

// Function to get the shopping list
function getShoppingList() {
    return shoppingList;
}
